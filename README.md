# salinewin-song
Implementation of the virus track salinewin.exe written in C

Written for Windows and MinGW, for compilation:
1. Clone the repository
2. Run the ```make``` command in the repository folder
